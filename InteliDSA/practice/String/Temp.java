package String;

import java.util.*;

public class Temp {
    public static void main(String[] args) {
        //LRU practice
        List<Integer> list=new ArrayList<>();
        for(int i=1;i<10;i++)list.add(i);
        //creting vertual ngnix server and upload custom data(custom html)
        //Exp : vertual serval+ngnix with user data script
    }
}
