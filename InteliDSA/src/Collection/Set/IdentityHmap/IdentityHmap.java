package Collection.Set.IdentityHmap;

public class IdentityHmap {
    //  is two values have same hashcode then hashmap replace the key with new one
    // becouse once it find it check .equals() methos which compares contect

    //but identity hasmap first here hashcode no it works on memory address
    // and it use == operator to compare which is again address comparator
}
