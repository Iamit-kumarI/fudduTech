package DP.DPTemplates;

/*
⭐ DP TEMPLATE 4 — Longest Increasing Subsequence (LIS) type
Used in:
LIS
LDS
bitonic subsequence
longest chain
envelope nesting
max subsequence sum
 */
public class LongestIncreasingSubsequence {
    //DP Style
    /*
    for(int i=0; i<n; i++){
    dp[i] = 1;
        for(int j=0; j<i; j++){
            if(arr[j] < arr[i]){
                dp[i] = Math.max(dp[i], dp[j] + 1);
            }
        }
    }
     */
}
